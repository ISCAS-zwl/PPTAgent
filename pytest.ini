[pytest]
# 保留失败测试的临时文件
tmp_path_retention_policy = failed

# 或者保留所有测试的临时文件
# tmp_path_retention_policy = all

# 保留最近N次测试的临时文件（默认3）
tmp_path_retention_count = 5

# 测试路径
testpaths = deeppresenter/test pptagent/test

# 测试文件模式
python_files = test_*.py

# 异步测试支持
asyncio_mode = auto
